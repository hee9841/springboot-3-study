spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: ture
    defer-datasource-initialization: true
  datasource:
    url: -
    username: -

  h2:
    console:
      enabled: true
#  sql:
#    init:
#      data-locations: import.sql
